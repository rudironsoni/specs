---
description: Check consistency across adr/, src/schema/, src/cli/, and site/ for the current branch before merging. Reports gaps between what the ADR declares, what the schema defines, what the CLI exposes, and what the docs cover.
---

Read and follow the instructions in `.github/agents/Specs.consistency-check.agent.md`.

$ARGUMENTS
