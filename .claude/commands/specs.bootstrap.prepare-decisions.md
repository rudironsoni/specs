---
description: Draft a decisions.yaml proposal from candidates. Do not treat the draft as accepted until a human writes it.
---

Read inventory, deterministic candidates, and agent candidates. Draft decision records using the RFC 002 enum. Print the draft. Do not overwrite `.specs/bootstrap/decisions/decisions.yaml` unless the user explicitly asks to write a draft file named `decisions.draft.yaml`.

$ARGUMENTS
