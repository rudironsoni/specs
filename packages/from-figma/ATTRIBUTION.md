# Attribution

This project uses `@rudironsoni/specs-schema` (MIT) and the MIT CLI contract.

The transformation engine in `src/` is a clean-room implementation by
rudironsoni. It was written from:

- the public schema and ADRs
- the MIT CLI call contract
- the leaked `0.22.0` npm type tree (method names and comments only)
