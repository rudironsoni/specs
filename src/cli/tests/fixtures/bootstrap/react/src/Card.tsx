import { PrimaryButton } from './PrimaryButton';

export function Card() {
  return (
    <div style={{ borderColor: '#1a74e8' }}>
      <PrimaryButton appearance="primary">Go</PrimaryButton>
    </div>
  );
}
