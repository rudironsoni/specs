{
  "private": true,
  "name": "specs",
  "workspaces": [
    "packages/schema",
    "packages/cli",
    "packages/from-figma"
  ],
  "scripts": {
    "build": "npm run build -w @directededges/specs-schema && npm run build -w @rudironsoni/specs-from-figma && npm run build -w @directededges/specs-cli",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^3.1.1"
  }
}
