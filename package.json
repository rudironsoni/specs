{
  "private": true,
  "name": "specs",
  "version": "0.31.0",
  "workspaces": [
    "src/schema",
    "src/from-figma",
    "src/cli"
  ],
  "scripts": {
    "build": "npm run build -w @rudironsoni/specs-schema && npm run build -w @rudironsoni/specs-from-figma && npm run build -w @rudironsoni/specs-cli",
    "test": "vitest run",
    "typecheck:from-figma": "tsc -p src/from-figma/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^3.1.1"
  }
}
