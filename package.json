{
  "private": true,
  "name": "specs",
  "workspaces": [
    "packages/schema",
    "packages/cli",
    "packages/from-figma"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^3.1.1"
  }
}
