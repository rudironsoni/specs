---
title: "bindings"
---

Generate Code Connect template files from accepted bootstrap bindings. Templates use the maintained Code Connect v2 format. This command does not publish to Figma.

```bash
specs bindings generate --workspace .specs/bootstrap --output ./code-connect
```
