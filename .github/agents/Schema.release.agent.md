---
description: Schema releases with the rest of Specs. Same version, same tag.
---

# Schema release

`@rudironsoni/specs-schema` is not released on its own. Follow `.github/agents/CLI.release.agent.md`.

A `feat(schema):` or `fix(schema):` on `main` still bumps schema, from-figma, and CLI together.
